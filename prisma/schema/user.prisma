model User {
    id        Int      @id @map("_id")
    username  String   @unique
    email     String   @unique
    firstName String
    lastName  String
    image     String
    password  String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
